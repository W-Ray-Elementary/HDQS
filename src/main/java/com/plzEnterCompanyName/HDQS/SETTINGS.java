package com.plzEnterCompanyName.HDQS;

/**
 * 这里的设置不能让用户或是开发者随便改，所以把它们编译入class文件中。
 */
public class SETTINGS {

    /**
     * 为了与国际接轨，本项目使用编码为 UTF-8.
     */
    public static final String ENCODE = "UTF-8";
}
